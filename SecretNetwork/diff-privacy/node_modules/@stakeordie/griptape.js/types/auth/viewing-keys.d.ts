import { BaseContract } from '../contracts/types';
import { AccountManager } from './account';
export declare class ViewingKeyManager extends AccountManager {
    constructor();
    add(contract: BaseContract, key: string): string;
    set(contract: BaseContract, key: string): void;
    get(idOrAddress: string): string | undefined;
    private createKey;
    private isEqual;
    private isKeyAdded;
}
export declare class KeplrViewingKeyManager {
    private readonly viewingKeyManager;
    constructor(viewingKeyManager: ViewingKeyManager);
    add(contract: BaseContract): Promise<string>;
    get(idOrAddress: string): string | undefined;
}
