import { BaseContract } from '../contracts/types';
import { AccountManager } from './account';
export declare class PermitManager extends AccountManager {
    constructor();
    get(contract: BaseContract): boolean;
    add(contract: BaseContract, permissions?: string[]): Promise<void>;
}
export declare function hasPermit(contract: BaseContract): boolean;
export declare function enablePermit(contract: BaseContract, permissions?: string[]): Promise<void>;
